# Template for secrets - DO NOT commit with real values
# Create actual secrets with:
# kubectl create secret generic embedding-service-secrets \
#   --from-literal=redis-url=redis://your-redis-host:6379

apiVersion: v1
kind: Secret
metadata:
  name: embedding-service-secrets
  labels:
    app: embedding-service
type: Opaque
stringData:
  redis-url: "redis://redis:6379"  # Replace with actual Redis URL
